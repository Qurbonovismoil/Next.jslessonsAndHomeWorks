import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Explicitly using standard settings to avoid path-related detection issues
};

export default nextConfig;
